{
  "name": "banjo-fretboard-visualizer",
  "version": "1.0.0",
  "description": "Banjo fretboard visualizer",
  "scripts": {
    "dev": "esbuild src/index.jsx --bundle --outdir=dist --servedir=dist --serve=:8080 --watch=forever",
    "build": "esbuild src/index.jsx --bundle --outdir=dist --minify"
  },
  "dependencies": {
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "esbuild": "^0.27.2"
  }
}
